# Checklist 04. `HO_CuratorPresenter`

## 문서/기획 확인
- [x] task 문서에 1차 내레이션과 상세 정보 2단계 구조가 분리되어 있다.
- [x] 플레이어 기준 2미터 위치 배치 규칙이 문서에 포함되어 있다.
- [x] 종료 시 플레이어 제어 복구 조건이 문서에 포함되어 있다.
- [ ] `단계별수행절차.md`에 큐레이터 오브젝트 생성과 참조 연결 순서가 반영되어 있다.

## 코드/구성 확인
- [x] `HO_CuratorPresenter.cs` 파일명과 클래스명이 일치한다.
- [x] 클래스와 주요 함수에 역할 설명 주석이 있다.
- [x] 현재 내레이션 대상과 단계 상태를 관리한다.
- [x] 첫 호출, 다음 호출, 종료 호출 흐름이 분리되어 있다.
- [x] 빈 내레이션 데이터에서 UI를 열지 않고 로그를 남긴다.
- [x] `CuratorVisualDummy`, `Capsule`, `NavMeshAgent`의 활성화나 설정값을 런타임에 자동으로 바꾸지 않는다.
- [x] `NavMeshAgent`는 인스펙터와 씬에 미리 설정된 값을 그대로 사용한다.
- [x] 플레이어 앞 2미터 목표가 NavMesh 밖이면 플레이어 위치를 fallback 목적지로 다시 샘플링한다.
- [x] `Ensure` 접두사 함수 없이 현재 동작이 구성되어 있다.

## 인스펙터 확인
- [ ] `HO_UIManager` 참조가 연결되어 있다.
- [ ] 플레이어 컨트롤러 참조가 연결되어 있다.
- [ ] 큐레이터 비주얼 루트 참조가 연결되어 있다.
- [ ] `NavMeshAgent`가 `CuratorPresenter` 오브젝트에 붙어 있다.
- [ ] `NavMeshAgent`의 `speed`, `stoppingDistance`, `baseOffset`, `updatePosition`, `updateRotation` 값이 의도대로 수동 설정되어 있다.

## Play Mode 검증
- [ ] 같은 작품에서 첫 `E` 입력 시 1차 내레이션이 나온다.
- [ ] 다음 입력 시 상세 정보 2단계가 나온다.
- [ ] 내레이션 대상이 바뀌면 이전 텍스트가 섞이지 않는다.
- [ ] 큐레이터 위치가 플레이어 기준 2미터 거리 규칙을 만족한다.
- [ ] 플레이어 앞 2미터 지점이 NavMesh 밖이어도 플레이어 위치 fallback으로 이동이 계속된다.
- [ ] 큐레이터 비주얼과 `Capsule`이 해설 시작/종료 중 스크립트 때문에 비활성화되지 않는다.
- [ ] 종료 시 내레이션 패널이 닫히고 플레이어 조작이 복구된다.
