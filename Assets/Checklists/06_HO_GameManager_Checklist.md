# Checklist 06. `HO_GameManager`

## 문서/정적 확인
- [x] task 문서에 전역 상태 변경을 `HO_GameManager` 한 곳에서만 수행한다고 적혀 있다.
- [x] 시작 시 공통 참조 누락 검증과 로그가 문서에 포함돼 있다.
- [x] 최종 인스펙터 연결 순서가 문서에 포함돼 있다.
- [x] `단계별수행절차.md`에 최종 공통 참조 연결과 마무리 점검 순서가 반영돼 있다.

## 코드/구성 확인
- [x] `HO_GameManager.cs` 파일명과 클래스명이 일치한다.
- [x] 클래스와 주요 함수에 역할 설명 주석이 있다.
- [x] 전역 상태 변경 메서드가 `HO_GameManager` 안에만 있다.
- [x] 플레이어 조작 잠금과 UI 상태를 함께 제어하는 메서드가 있다.
- [x] 참조 누락 시 경고 로그를 남기는 초기 검증 로직이 있다.

## 인스펙터 확인
- [ ] `HO_PlayerController`, `HO_UIManager`, `HO_CuratorPresenter`, `HO_ExhibitionFlowController` 참조가 모두 연결돼 있다.
- [ ] 초기 상태가 프롤로그 또는 지정한 시작 상태로 설정돼 있다.

## Play Mode 검증
- [ ] 미검증: 해설 UI가 열리면 플레이어 조작이 잠긴다.
- [ ] 미검증: 해설 UI가 닫히면 플레이어 조작이 다시 풀린다.
- [ ] 미검증: 종료 상태에서는 추가 안내보다 종료 메시지가 우선된다.

## 구현 메모
- [x] `HO_GameManager`가 프로토타입용 전시 상태 스냅샷과 공통 참조 보관 역할을 가진다.
- [x] `Awake()`에서 공통 참조 검증, 초기 UI 정리, 첫 잠금 상태 동기화를 수행한다.
- [x] `Update()`에서 UI 차단 상태, 큐레이터 프레젠테이션 상태, 종료 상태를 합쳐 플레이어 잠금을 공통 제어한다.
