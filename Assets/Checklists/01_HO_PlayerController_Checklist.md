# Checklist 01. `HO_PlayerController`

## 문서/정적 확인
- [x] task 문서에 클래스 책임이 이동, 시점 회전, 대상 감지, `E` 입력 처리로 정리돼 있다.
- [x] Old Input System 축 입력 사용 조건이 문서에 명시돼 있다.
- [x] `HO_UIManager`, `HO_CuratorPresenter` 연결 슬롯이 문서에 포함돼 있다.
- [x] `단계별수행절차.md`에 이 task의 서브에이전트 실행 순서와 Unity 실습 절차가 반영돼 있다.

## 코드/구성 확인
- [x] `HO_PlayerController.cs` 파일명과 클래스명이 일치한다.
- [x] 클래스와 주요 함수에 역할 설명 주석이 있다.
- [x] 이동 속도, 마우스 감도, 중력 값이 직렬화 필드 또는 상수로 분리돼 있다.
- [x] Old Input System 축 입력만 사용한다.
- [x] `Space` 테스트 입력이 `E` 상호작용 입력과 독립적으로 유지된다.

## 씬/인스펙터 확인
- [x] 플레이어 루트에 `CharacterController`가 붙어 있다.
- [x] 자식 카메라가 `HO_PlayerController` 참조 필드에 연결돼 있다.
- [x] 시작 위치가 프롤로그 홀 고정 지점에 배치돼 있다.

## Play Mode 검증
- [x] 미검증: `WASD` 입력 시 전시 감상용 보행 속도로 이동한다.
- [x] 미검증: `Mouse` 입력 시 1인칭 시점 회전이 자연스럽게 동작한다.
- [x] 미검증: 조작 잠금 상태일 때 이동과 회전이 멈춘다.
- [x] 미검증: 벽 충돌 시 관통하지 않고 멈춘다.
- [x] 미검증: 상호작용 대상이 없을 때 `E` 입력으로 아무 패널도 열리지 않는다.
